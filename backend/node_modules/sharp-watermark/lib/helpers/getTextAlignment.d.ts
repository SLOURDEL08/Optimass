import type { TextAlign } from "sharp";
import type { Options } from "./types";
declare const getTextAlignment: (position: Exclude<Options["position"], undefined>) => TextAlign;
export default getTextAlignment;
